name: tarefa_methodchannel
description: Exemplo com MethodChannel
version: 1.0.0+1
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"     # Dart 3.7.0 está dentro deste intervalo
  flutter: ">=3.16.0"       # Ajuste se quiser exigir uma mínima do Flutter

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
